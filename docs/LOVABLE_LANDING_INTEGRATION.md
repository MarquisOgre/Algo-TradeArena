# Lovable landing integration source

The source landing project is `MarquisOgre/Alphentra-Landing` on `main`.

Key source files inspected:
- src/routes/index.tsx — complete homepage composition and section anchors
- src/styles.css — homepage visual system and responsive layout
- src/routes/__root.tsx — fonts, root shell, and CSS loading
- src/assets/alphentra-world.png.asset.json — hero/closing artwork reference

Integration branch:
`Lovable-Landing-Integration`

The production Alphentra application remains under the `Alphentra` repository and the `Landing` branch remains the UI working branch.